# Conformance modules that must pass. Anything listed here is blocking in CI.
#
# Empty, and not because nothing passes.
#
# The lane's results are currently not reproducible from a clean clone: a working tree carrying
# hand-applied fixes produced 53 passed / 11 failed, while `rm -rf var/conformance` followed by
# the same run produces 2 passed / 62 failed, every failure downstream of an empty capability
# intersection. `scripts/run-conformance.sh` now applies the patches in docs/upstream/ so the
# committed ones are at least accounted for, and that alone does not close the gap.
#
# Enforcing a module before the difference is understood would make CI fail for a reason nobody
# can act on, which is worse than not gating at all. Fill this in once a clean clone reproduces
# a stable number twice in a row; docs/conformance.md records where that stands.
