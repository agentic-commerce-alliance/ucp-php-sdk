# Conformance modules that must pass. Anything listed here is blocking in CI.
#
# These are green from a clean clone, twice in a row with identical results. That reproducibility
# is the precondition: an earlier attempt to enforce was reverted because the numbers came from a
# working tree carrying hand-applied fixes, and CI clones fresh. `scripts/run-conformance.sh` now
# hard-resets the checkout and applies docs/upstream/*.patch itself, so a local run and a CI run
# see the same suite.
#
# A module is NOT listed while it fails for a reason outside this repository. Blocking on
# something we cannot fix teaches people to ignore a red check; docs/conformance.md records what
# stands between each unlisted module and this file.
checkout_lifecycle_test.py
discount_test.py
protocol_test.py
simulation_url_security_test.py
totals_test.py
validation_test.py
webhook_test.py
